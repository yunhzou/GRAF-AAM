# Controlled Golden evaluation: cap 2,000 with cap-100 ablations

Status: RUNNING on NRT CPU-short. This report contains the submitted protocol and source snapshot, not final performance results. The manuscript remains on the previously verified cap-100 results until the new campaign has been validated.

Twelve configurations, all 1,851 Golden reactions and both directions: GRAFT at caps 100 and 2,000, each with 1/2/3/10 swept seed orders and a one-seed no-sweep control; SLAP with and without our sweep, unioning binary and weighted modes. Root seed 42, iso tolerance 1, graph floor 0.2. Each reaction's configurations run serially in deterministic shuffled order on one pinned CPU, with numerical libraries restricted to one thread. All observed nodes initially use AMD EPYC 9J14 processors. Five-minute external watchdogs apply independently to search and reference verification, with 6 GiB worker RSS and 8 GiB Slurm memory.

Search workflow CPU covers graph preparation, mapping and output persistence, excluding reference verification and initial process imports. Full worker CPU including startup is recorded separately. GRAFT retains raw compressed cut graphs; SLAP retains native label information and explicit mapped candidates. Symmetry finalization and reference verification are outside the primary search timer. Report a shared completed-case cohort across all 12 configurations, as well as all-attempted CPU and resource-limit counts; never pool old Mac and new Linux timings or treat interrupted calls as completed-search timings.

The source snapshot is commit 1ba2d37 plus the documented one-line checkpoint-verifier fix for sweep_cuts=False. The native build passed 8 focused tests; the small Linux pilot passed all 12 configurations in both directions on cases 9 and 1001, including exact agreement with archived cap-100 recovery verdicts. A local checkpoint-verifier regression suite passed 16 tests. Local smoke timings are excluded.

Initial oversized pilot 6817888 and its blocked arrays 6817903/6817904 were canceled. Its case 0 checkpoints remain under pilot-partial/case0 and are excluded from this campaign's measurements. The replacement pilot 6818073 passed. Main arrays 6818088 and6818089 cover cases 0–999 and1000–1850; each allows 50 concurrent tasks. One requested CPU allocates two on NRT, so their combined limit is 200 allocated CPUs, below the authorized 300. Only this campaign's jobs are managed.

Remote directory: /lustre/fsw/portfolios/nvr/users/yunhengz/Projects/coordinate_alignment/golden_controlled_20260915 . Runtime and label-free inputs come from sibling validation_20260912. Use AAM_VALIDATION_BASE when relocating. The payload contains the frozen source, inputs manifests, SLAP adapter and original build scripts; array_shard.sbatch and small_pilot.py are the final scheduler/gate revisions, recorded separately here. Results and raw checkpoints remain in the remote results directory. Do not submit scripts without adapting project paths and inspecting their resource settings.

Final aggregation job 6818580 waits for both main arrays. It creates comparison.csv, directional.csv, COMPARISON.md, controlled-results.json.gz and a runtime proof only after all 1,851 tasks are processed and CPU-model/thread consistency passes. Missing tasks block final tables; incomplete searches remain explicitly counted. The manuscript and Git results require a subsequent review; no paper publication is automatic.
