# Golden case 15: growth, fragment competition and canonical event decoding

The 28-second research preview uses real saved GRAFT search states and a verified competition completion. It replaces the coordinate-case illustration with a Golden example. The source is one-seed, cap-100, iso-tolerance-1 sweep output. The displayed baseline paths are terminals 29 and 30 of context 2, with the N0–H19 sweep constraint. Fragment-growth calls were replayed against their archived placements and matched exactly.

The displayed competition starts from terminal 29. The O18 fragment receives priority to extend across C16: O18 changes target, displacing O17, while H19 is also released. The fresh completion retains 28 anchors and refills two assignments. It preserves the original baseline paths. This is a new conditioned completion, not ordinary depth-first backtracking. `offers.json` records the actual proposal and `takeover3.json.gz` records its graph, including the unchanged-assignment revalidation step that is omitted from the compact diagram.

The three displayed terminal witnesses A, B and C map to two canonical event classes: B and C merge. The closing representatives come directly from decoding final-catalogue families 4 and 5, keeping the atom identities used earlier in the animation. The first class is equivalent to the Golden reference under the benchmark's charged heavy-atom graph criterion. The second is a different returned alternative; its existence does not establish a physical pathway.

The saved catalogue combines the entire one-seed sweep with a bounded illustrative competition pass: 32 operations, three parents, one depth, a 20-second soft limit, and the library's default dependent-component policy. It has 58 unordered fragment branches and 118 families. Every family is completely decoded through six events, yielding nine canonical classes. The video shows two classes and explicitly retains the other seven in its summary. These numbers describe this saved catalogue, not an exhaustive search over every possible mapping. Competition itself took about 0.06 CPU seconds locally; preparation and complete symbolic decoding took about 1.3 CPU seconds. These are illustration preparation costs, not new benchmark timings.

`input.json` preserves the search input, whose coordinates are placeholders. `film-source.json` additionally contains illustrative 3D conformers built with RDKit ETKDG (seed 42) and UFF, preserving exact atom indexing, elements and bond orders. They are display geometry, not measured Golden geometries or a reaction trajectory.

Run `python reports/golden_film_20260915/replay_decode.py` in the repository environment to verify the takeover anchors, saved completion, nine-class decode, displayed witnesses and reference label. It performs no new AAM search. `trace.json` stores the detailed baseline replay; `baseline.json.gz` and `takeover3.json.gz` are portable archive records; `catalogue.json.gz` and `decode.json` preserve the final family catalogue and completion certificates. `film-source.json` pins the source archive hashes and producing commit.
