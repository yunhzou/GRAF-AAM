# Importable Python API audit — 2026-09-14

Added the executed docs/AAM_SIMPLE.ipynb and docs/PYTHON_API.md. The notebook uses six explicit atoms, one worker, and formal bond orders; its mappings are demonstration outputs, not a chemical reference. A freshly pip-built/installed package without the optional native growth engine executed all eight code cells in 1.77 seconds. Both py3Dmol viewer grids rendered in headless Chrome, with two shared canvases and zero JavaScript errors. The screenshot records visual inspection after separating disconnected product components.

New Python entry points: search_aam_directions; rxn_core.postprocessing.decode_events, EventDecodeConfig, DecodedEvents.symmetry/query; rxn_core.competition.compete_fragments and CompetitionConfig. Added random_seed and sweep_cuts controls to AAMSearchConfig, preserving default seed 42 and cut sweep, including legacy default checkpoint identity. Dependency extras notebook and postprocessing are declared in pyproject.toml. Search and event decoding remain separate.

Six public-workflow tests plus sixty existing search/event/family tests pass. Tests cover same-event versus unrestricted saved-family shuffles, conditional assignments, explicit unknown outcomes, thresholds without rerunning search, anchors under endpoint inversion, unequal endpoints, checkpoint identity, nondefault seed/cut behavior across backends, and an accepted competition repair with hard anchors. The reference-growth workflow also passed (native-only tests skipped). Native binaries were reused from the existing local build for native tests; fresh package installation independently tested the ordinary Python growth path.

The broader test run initially lacked benchmark fixture modules in the temporary staging tree; supplying the repository fixture paths resolved collection/import errors. A new backend parity assertion was corrected to compare event families for shared_policies, which intentionally stores a different shared DAG; exact DAG equality remains checked for reused_native. No algorithm failure was hidden as a skipped test.

Competition was extracted from the fixed local-collision policy in bench/experiments/competition_checkpoint/fragment_competition_optimized.py. The adapter removes machine paths, campaign IO, and reference scoring; it returns raw accepted repair families, enforces supplied hard anchors, and exposes budgets. It was exercised on a small reaction with a nonempty accepted repair. No 140-case campaign was rerun, and this audit does not claim fresh full-campaign parity.

Known limits documented: general stereochemical AAM preservation is TODO; coordinate/index-chirality selection remains downstream. The optional shared_policies backend rejects anchors. Bidirectional search retains two directed compressed outputs; a unified exact cross-direction event quotient is not implemented. The signed-event decoder requires balanced complete mappings, while raw AAM supports partial/unbalanced inputs. Solver budgets are soft and incomplete status is explicit.
