# Numerical paper evidence

The four snapshots listed in `paper_sources.json` are generated from the fresh final-version benchmark report. See `PAPER_EVIDENCE.md` for their scope and the report for per-case execution records. They are immutable inputs to paper builds: regenerate them from completed experiments when numerical results change. A figure build never launches matching or decoding.
