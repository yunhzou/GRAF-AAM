We measured one-seed, cap-2,000 cut-sweep search followed by the public event decoder on the Apple M4 Pro CPU. Four reactions ran concurrently, with one numerical thread per reaction. CPU times are per reaction, not divided by the worker count. The event thresholds and windows match the bond-event comparison, with no class-count cap. All 140 completed reactions reproduce the archived event-class sets and family counts. Median end-to-end wall time is 0.96 seconds. Timing includes input loading, checkpoint I/O, final-family construction, decoding, candidate serialization, and progress recording; initial imports and reference-set verification are excluded. All 140 reactions finish within the five-minute watchdog. Per-case timings, source hashes, and reproduction commands are provided with the code.
